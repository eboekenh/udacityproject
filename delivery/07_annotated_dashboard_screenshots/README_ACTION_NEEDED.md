# Action needed: add screenshots of the 4 dashboard annotations

The dashboard itself already has 4 text-box annotations across its sheets (including the corrected ROI annotation on the Marketing Funnel sheet — see `03_dashboard_pdf_export/README_ACTION_NEEDED.md`), but this folder needs dedicated screenshots of them for the "Annotated dashboard screenshots" deliverable (the rubric wants to clearly see 3–5 quantified-finding annotations on the sheets, not just infer them from a full-page PDF export).

**To fix:** after re-exporting the dashboard PDFs (see the note in `03_dashboard_pdf_export/`), take one screenshot per sheet that shows the annotation text box(es) clearly and legibly (zoom in if needed so the text is readable), and save them here, e.g.:

- `marketing_funnel_annotation.png`
- `sales_pipeline_annotation.png`
- `customer_health_annotation_1.png`
- `customer_health_annotation_2.png` (if there are two on that sheet)

Each annotation should already state a quantified finding, why it matters, and a recommended action — per Verification 5 in the rubric, that's what to double check is visible in each screenshot.
